import { Metadata } from "next";

const metadata: Metadata = {
  title: "Login",
};

const CustomersPage = () => {
  return (
    <div>
      <p>Customers Page</p>
    </div>
  );
};

export default CustomersPage;
